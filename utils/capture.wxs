/**
 * 字符串截取
 * 
 * @param {string} str 字符串
 * @param {number} start 起始位置 
 * @param {number} stop 结束位置
 */
function subStr(str, start, stop) {
    if (str) {
        return str.substring(start, stop)
    }
    return ''
}


module.exports = {
    subStr: subStr
}